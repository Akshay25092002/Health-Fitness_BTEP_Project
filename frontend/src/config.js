export const BASE_URL = "http://localhost:5000/api/v1";
export const token = localStorage.getItem("token");
//BASE URL- basically our Api url same endpoint for all the url

//you can use AuthContext instead of local storage but i just use like this
